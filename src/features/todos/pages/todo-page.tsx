import { SiteHead } from '@/components';
import React from 'react';

interface TodoPageProps {}

const TodoPage: React.FC<TodoPageProps> = ({}) => {
  return <SiteHead title="Todos">TodoPage</SiteHead>;
};

export default TodoPage;
